# Existing Code

Only used for edit jobs — leave empty for new builds.

**What goes here:**
- The current website codebase (clone or copy the repo here)
- Or just the src/ folder if the full repo is too large

**Tips:**
- The agent will read the existing code first and only change what's needed
- Include package.json so the agent knows the current stack and dependencies
- If it's a Vercel/Netlify project, include any config files
